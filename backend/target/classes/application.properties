# ===========================================
# SERVER CONFIGURATION
# ===========================================
# Development server settings
server.port=${SERVER_PORT:8080}
server.servlet.context-path=${SERVER_CONTEXT_PATH:/}

# Production server settings (override if needed)
# SERVER_PORT=8080
# SERVER_CONTEXT_PATH=/

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
# spring.datasource.url=${DATASOURCE_URL}
# spring.datasource.username=${DATASOURCE_USER}
# spring.datasource.password=${DATASOURCE_PASSWORD}
# spring.datasource.driver-class-name=${DATASOURCE_DRIVER_CLASS_NAME}
# spring.jpa.database-platform=${JPA_DATABASE_PLATFORM}



# PostgreSQL Database Configuration (Neon Cloud)
spring.datasource.url=${DATASOURCE_URL:jdbc:postgresql://ep-twilight-surf-ae34ehdf-pooler.c-2.us-east-2.aws.neon.tech:5432/neondb?sslmode=require&channel_binding=require}
spring.datasource.username=${DATASOURCE_USER:neondb_owner}
spring.datasource.password=${DATASOURCE_PASSWORD:npg_clqEm9Xeauf8}
spring.datasource.driver-class-name=${DATASOURCE_DRIVER_CLASS_NAME:org.postgresql.Driver}
spring.jpa.database-platform=${JPA_DATABASE_PLATFORM:org.hibernate.dialect.PostgreSQLDialect}
# ===========================================
# JPA CONFIGURATION
# ===========================================
spring.jpa.hibernate.ddl-auto=${JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.show-sql=${JPA_SHOW_SQL:false}
spring.jpa.properties.hibernate.format_sql=${JPA_FORMAT_SQL:false}

# ===========================================
# FRONTEND CONFIGURATION
# ===========================================
frontend.url=${FRONTEND_URL}

# ===========================================
# FILE UPLOAD CONFIGURATION
# ===========================================
spring.servlet.multipart.max-file-size=${FILE_UPLOAD_MAX_FILE_SIZE:10MB}
spring.servlet.multipart.max-request-size=${FILE_UPLOAD_MAX_REQUEST_SIZE:10MB}

# ===========================================
# CORS CONFIGURATION
# ===========================================
# Development CORS origins
spring.web.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:3001,http://localhost:5173,https://*.vercel.app,https://*.netlify.app,https://*.github.io,https://*.railway.app,https://*.herokuapp.com,https://*.render.com}
spring.web.cors.allowed-methods=${CORS_ALLOWED_METHODS:GET,POST,PUT,DELETE,OPTIONS}
spring.web.cors.allowed-headers=${CORS_ALLOWED_HEADERS:*}

# ===========================================
# FRONTEND URL CONFIGURATION
# ===========================================
frontend.url=${FRONTEND_URL:http://localhost:3000}

# ===========================================
# LOGGING CONFIGURATION
# ===========================================
logging.level.com.ideafactory=${LOGGING_LEVEL_COM_IDEAFACTORY:INFO}
logging.level.root=${LOGGING_LEVEL_ROOT:WARN}

# ===========================================
# SECURITY CONFIGURATION
# ===========================================
jwt.secret=${JWT_SECRET:your-super-secret-jwt-key-change-this-in-production}
jwt.expiration=${JWT_EXPIRATION:86400000}

# ===========================================
# H2 CONSOLE (Development Only)
# ===========================================
spring.h2.console.enabled=${H2_CONSOLE_ENABLED:false}
spring.h2.console.path=${H2_CONSOLE_PATH:/h2-console} 